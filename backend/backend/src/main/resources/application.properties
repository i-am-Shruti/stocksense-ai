spring.application.name=backend

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

server.port=${PORT:8080}

jwt.secret=${JWT_SECRET:stocksense_super_secret_key_2026_do_not_share}
jwt.expiration=86400000

ml.service.url=${ML_SERVICE_URL:https://stocksense-ai-bdd9.onrender.com}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:grimjhim153@gmail.com}
spring.mail.password=${MAIL_PASSWORD:mwnewzgygqqyzfnx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

app.name=StockSense AI
mail.from.name=StockSense AI - New User Registration

logging.level.com.stocksense=DEBUG